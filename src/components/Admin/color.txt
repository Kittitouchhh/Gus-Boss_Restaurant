[#FFEED9] - cream
[#684A3A] - Brown
[#a67c52] - BrownLight
[#fba542] - Orange cool


{/* จุดขาวล่าง (slide indicator) */}
          <div className="absolute bottom-4 left-1/2 -translate-x-1/2 flex gap-2">
            {[...Array(5)].map((_, i) => (
              <div
                key={i}
                className={`w-3 h-3 rounded-full ${
                  i === 0 ? "bg-white" : "bg-white/40"
                }`}
              ></div>
            ))}
          </div>